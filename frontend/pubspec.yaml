name: securebypay_frontend
description: Myafrimall shipping dashboard (technical assessment).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  http: ^1.2.1
  flutter_secure_storage: ^9.2.2
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  intl: ^0.19.0
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
